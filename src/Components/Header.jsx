import React from "react";

const Header = () => {
    return (
        <div className={"row"}>
            <p className={"page-header"}>Drag&Drop in Space </p>
        </div>
    );
};

export default Header;